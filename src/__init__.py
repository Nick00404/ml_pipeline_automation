# __init__.py
# This file initializes the 'src' package and its submodules for structured access.

# Expose key modules for easier imports
from . import data
from . import model
from . import utils

# Optionally, expose specific functions/classes if needed
# from .data.data_ingestion import load_dataset, split_dataset
# from .model.model_training import train_classifier
